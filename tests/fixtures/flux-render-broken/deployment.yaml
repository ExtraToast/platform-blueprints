apiVersion: apps/v1
kind: Deployment
metadata:
  name: broken-api
  namespace: flux-render-broken
spec:
  replicas: definitely-not-a-number
  selector:
    matchLabels:
      app.kubernetes.io/name: broken-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: broken-api
    spec:
      containers:
        - name: api
          image: registry.k8s.io/pause:3.10
